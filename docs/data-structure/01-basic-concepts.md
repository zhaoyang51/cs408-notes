# 一、基本概念

> 本章包含考点小节：2 个

---

# （一）数据结构的基本概念

## 数据结构的基本概念

### 1. 基本术语与概念

- **数据（Data）**：客观事物的符号表示，是所有能输入到计算机中并被计算机程序处理的符号的总称。

- **数据元素（Data Element）**：数据的基本单位，通常作为一个整体进行考虑和处理。由若干个数据项（Data Item）组成（数据项是组成数据元素的不可分割的最小单位）。

- **数据对象（Data Object）**：具有相同性质的数据元素的集合，是数据的一个子集。

### 2. 数据结构三要素

- **逻辑结构**：反映数据元素之间的逻辑关系，与存储方式无关。

- **线性结构**：一对一关系（线性表、栈、队列、数组、字符串）。

- **非线性结构**：集合、树形结构（一对多）、图形结构/网状结构（多对多）。

- **存储结构（物理结构）**：数据结构在计算机中的表示（又称映像）。

- **顺序存储**：把逻辑上相邻的元素存储在物理位置上也相邻的存储单元中。

- **链式存储**：借助指示元素存储地址的指针来表示数据元素之间的逻辑关系。

- **索引存储**：在存储元素信息的同时，建立附加的索引表。

- **散列存储（Hash）**：根据元素的关键字直接计算出该元素的存储地址。

- **数据的运算**：包括运算的定义和实现。运算定义针对逻辑结构，运算实现针对存储结构。

### 3. 抽象数据类型（ADT）

- **定义**：抽象数据类型是指一个数学模型以及定义在该模型上的一组操作。

- **特征**：数据抽象与信息隐藏，只关心逻辑特性，不关心具体存储实现。


---

# （二）算法的基本概念

## 算法的基本概念

### 1. 算法的定义与 5 个重要特性

- **有穷性（Finiteness）**：算法必须在执行有穷步之后结束，且每一步都可在有穷时间内完成。

- **确定性（Definiteness）**：算法中每一条指令必须有确切的含义，读者理解时不会产生二义性。对于相同的输入只能得出相同的输出。

- **可行性（Effectiveness）**：算法中描述的操作都可以通过已经实现的基本运算执行有限次来实现。

- **输入（Input）**：一个算法有零个或多个输入。

- **输出（Output）**：一个算法有一个或多个输出。

### 2. 好算法的目标

- **正确性（Correctness）**：算法应能正确地解决求解问题。

- **可读性（Readability）**：良好的可读性有助于人们理解算法。

- **健壮性（Robustness）**：当输入非法数据时，算法能恰当地做出反应或进行处理。

- **高效率与低存储量需求**：执行时间短，所需最大存储空间小。

### 3. 算法时间复杂度与空间复杂度分析

- **时间复杂度 T(n)**：所有语句的频度之和 T(n) = O(f(n))。常见阶数：O(1) < O(log2 n) < O(n) < O(n log2 n) < O(n^2) < O(n^3) < O(2^n) < O(n!) < O(n^n)。

- **最坏、最好与平均时间复杂度**：408 中重点考查最坏时间复杂度与平均时间复杂度。

- **空间复杂度 S(n)**：衡量算法在运行过程中临时占用的存储空间大小。原地工作（In-place）指算法所需辅助空间为 O(1)。


---
