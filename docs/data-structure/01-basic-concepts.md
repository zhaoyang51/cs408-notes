# 第1章 基本概念与时空复杂度

<span class="badge badge-star">⭐️ 全科必考第 1 题</span>
<span class="badge badge-freq">📊 考频 22 次（选 10 / 简 12）</span>
<span class="badge badge-formula">💡 算法大题必带分析</span>

---

## 1. 数据结构三要素

- **逻辑结构**（与硬件实现无关）：
- **线性结构**：线性表、栈、队列、数组、字符串
- **非线性结构**：集合、树（一对多）、图（多对多）
- **物理结构（存储结构）**：顺序存储、链式存储、索引存储、散列存储（Hash）
- **数据运算**：运算定义（基于逻辑结构）+ 运算实现（基于物理结构）

---

## 2. 算法 5 大特性与好算法评价

| 特性 | 含义与判定标准 | 易错点 |
|:---|:---|:---|
| **有穷性** | 算法必须在执行有限步和有限时间内结束 | 死循环不是算法，程序可以无穷（如操作系统） |
| **确定性** | 每条指令含义明确，无二义性；相同输入必产生相同输出 | 不允许歧义操作 |
| **可行性** | 算法中操作都可以通过基本运算有限次实现 | 必须可在物理机器上完成 |
| **输入** | $\ge 0$ 个输入 | 可以没有输入（内置数据） |
| **输出** | $\ge 1$ 个输出 | **至少有 1 个输出**（无输出的算法无意义） |

---

## 3. 渐近时间复杂度分析

<KP author="Zhao" title="阶数大小比较（必背口诀）" tag="💡 核心速记">

$$
O(1) \lt O(\log_2 n) \lt O(n) \lt O(n \log_2 n) \lt O(n^2) \lt O(n^3) \lt O(2^n) \lt O(n!) \lt O(n^n)
$$

</KP>

<KP author="Chen" title="主定理 (Master Method) 递归分治秒杀公式" tag="⭐️ 技巧">

对于递推形式 $T(n) = a T(n/b) + O(n^d)$，比较 $\log_b a$ 与 $d$：
1. **若 $\log_b a \gt d$**：$T(n) = O(n^{\log_b a})$
2. **若 $\log_b a = d$**：$T(n) = O(n^d \log n)$
3. **若 $\log_b a \lt d$**：$T(n) = O(n^d)$

> 典型例题：归并排序 $T(n) = 2T(n/2) + O(n)$，$\log_2 2 = 1 = d \Rightarrow O(n \log n)$。

</KP>

::: tip ⚠️ 经典命题陷阱
1. **循环变量倍增**：`for(int i=1; i<n; i*=2)` 执行次数为 $\lfloor \log_2 n \rfloor + 1$，复杂度为 $O(\log_2 n)$。
2. **递归调用**：必须写出递推关系式（如 $T(n) = 2T(n/2) + O(n) \Rightarrow O(n \log n)$）。
3. **原地工作 (In-place)**：空间复杂度为 $O(1)$，并非不消耗内存，而是辅助空间不随输入规模 $n$ 增长。
:::

<KP author="Zhao" title="真题考点自测：递推时空复杂度展开" tag="🎯 考点自测">

**【题目】** 已知某算法递推式为 $T(n) = T(n-1) + n$，求时间复杂度？

**【解析】** 通过累加展开：$T(n) = T(n-2) + (n-1) + n = \dots = T(1) + \sum_{i=2}^n i = O(n^2)$。

</KP>


